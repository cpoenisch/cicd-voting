apiVersion: v1
kind: Service
metadata:
  labels:
    app: cicd-voting
  name: cicd-voting
spec:
  type: NodePort
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: cicd-voting
